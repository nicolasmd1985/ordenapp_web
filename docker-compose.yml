version: '3'
services:
  redis:
    image: redis:6.2
    networks:
      - app_network
  web:
    #for production dont use volumes
    # network_mode: "host"      
    # volumes:
    #   - .:/app
    # build: .
    image: nicolasmd/ordenappweb:latest # Use the pre-built image
    container_name: ordenapp_web_container
    ports:
      - "3000:3000"
    depends_on:
      - redis
    env_file:
      - .env.${ENV} # This should be .env.production on the server
    environment:
      TZ: America/New_York
      RAILS_ENV: ${ENV}
      RAILS_SERVE_STATIC_FILES: "true"
      RAILS_LOG_TO_STDOUT: "true"
      RAILS_RELATIVE_URL_ROOT: /ordenapp
    volumes:
      - /home/ubuntu/deploy/public:/app/public:ro
    networks:
      - app_network
    tty: true
    stdin_open: true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  app_network:
    driver: bridge